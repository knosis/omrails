<tr>
    <td><%= precap.description %></td>
    <td><%= link_to 'Show', precap %></td>
    <%= if current_user == precap.user %>
	    <td><%= link_to 'Edit', edit_precap_path(precap) %></td>
	    <td><%= link_to 'Destroy', precap, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <% end %>
  </tr>